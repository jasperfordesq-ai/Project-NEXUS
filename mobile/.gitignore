node_modules/
.expo/
.codex-logs/
.apk-audit/
audit-screenshots/
dist/
web-build/
android/
ios/
*.orig.*

# env
.env
.env.local
.env.*.local

# macOS
.DS_Store

# EAS
eas-build-local-nodejs/
google-play-key.json
google-services.json
GoogleService-Info.plist
fcm-service-account*.json
firebase-service-account*.json
*-service-account*.json
*.keystore
*.jks

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
emu*.png

# Screenshot testing: the BASELINE is the assertion and must be committed.
# `current/` and `diff/` are per-run output and must not be — committing them
# would make every capture a diff and bury the one that matters.
screenshots/current/
screenshots/diff/
