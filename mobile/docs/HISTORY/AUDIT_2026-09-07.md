<!--
Copyright © 2024–2026 Jasper Ford
SPDX-License-Identifier: AGPL-3.0-or-later
Author: Jasper Ford
See NOTICE file for attribution and acknowledgements.
-->

# Native app audit — 2026-09-07

Last reviewed: 2026-09-07

Status: **Historical record of one audit pass. The live backlog is in
[`../CURRENT_MOBILE_PRODUCTION_STATUS.md`](../CURRENT_MOBILE_PRODUCTION_STATUS.md); do not
update this file when a finding below is later fixed — update the status document.**

## What was audited, and how

The owner asked for a full audit of `mobile/` towards production readiness: bugs, UI/UX
polish, route tracing, module gating, completeness. This pass ran the day after the
2026-09-05/06 audit, so it deliberately did not re-report anything that audit fixed
(`CHANGELOG.md`, `[Unreleased]`).

Evidence tiers, honestly stated:

- **Source evidence.** Every finding below was verified against the current code by a
  second read before it was fixed; every fix has a Jest test that was observed failing
  before and passing after, or extends a test that already exercised the path.
- **Mechanical checks.** Type-check, lint, the 416-suite Jest run, the drift/parity/API
  ledger, the untranslated-phrase ceiling, release configuration, network security policy,
  production dependency audit, Play asset validation and the startup budget all passed on
  the pre-audit tree and again after the fixes (a bundle was NOT rebuilt, so the budget
  figure is the previous build's).
- 🔴 **No device evidence.** Nothing here was walked on an emulator or a phone. Layout and
  gesture findings from the auditors are recorded as such and were fixed only where the
  code made the fault unambiguous.

Coverage of this pass (the app has 167 modal screens, 10 tab screens, 7 auth screens):

| Area | Read in full | Report |
| --- | --- | --- |
| Tabs, auth, contexts, hooks, API client, core components | yes | A — 2 P1, 6 P2, 14 P3 |
| Messaging, wallet, exchange workflow, profile, settings, notifications | yes | B — 5 P1, 12 P2, 14 P3 |
| Events (10 screens + 7 components), groups (7 screens) | yes | C — 4 P1, 14 P2, 13 P3 |
| Marketplace (~30 screens, Stripe) | yes, on a second attempt | D — 6 P1, 11 P2, 6 P3 |
| Volunteering, jobs, organisations | no | — |
| Gamification, goals, polls, reviews, endorsements, blog, resources, ideation, clubs, venues | no | — |
| Courses, podcasts, federation | no (module-by-module read done 2026-09-06) | — |

## Cross-cutting finding: module gating

The React app wraps about 150 routes in `<FeatureGate>`; the native app gated its menus and
five authoring screens. A deep link, a push notification, a shared URL or a screen pushed
from another screen reached every other module regardless of the community's switches.

Fixed: `lib/navigation/routeRequirements.ts` is one table of screen → required feature or
module, read by `components/withRouteGate.tsx` (wraps every screen's default export),
`app/(tabs)/_layout.tsx` (hides a tab whose module is off) and the deep-link store. Unknown
configuration (cold start, offline, tests) allows. `app/routeGating.test.ts` fails when a
new screen has no recorded decision, and `routeRequirements.test.ts` proves the deep-link
store and the screen gate agree for every gated route. Home is deliberately ungated (it is
the anchor every redirect lands on) and gates its own feed section.

Still a courtesy, not a boundary: the API does not enforce `courses` or `podcasts`
(recorded 2026-09-06, still open, owner decision).

## The marketplace, audited separately

The first marketplace auditor was killed by the account's session usage limit with nothing
written. A second, run alone, read the module in full. It found no crash or data-loss class
and confirmed the order idempotency key and its server-side fingerprint replay are sound —
but six P1 findings on the money path, all fixed here:

- **Checkout never showed a total.** The item price was at the top of the screen, each
  delivery option carried its own price, and an applied coupon showed no figure at all
  because the server's discount amount was discarded. A card buyer first saw the real charge
  in the Stripe sheet; a time-credit buyer never saw one.
- **A time-credit or free purchase committed on one tap.** The server debits the wallet as
  the order is created, so the tap on "Buy now" *was* the purchase.
- **A payment that went through could be reported as failed.** If the app's confirm call
  failed after Stripe had charged the card, the member was told "Payment failed" and could
  pay again.
- **Cancelling the payment sheet gave the wrong instruction** and stranded an unpaid order
  with no route to it; the checkout key was also never cleared after a completed order.
- **"Free" was shown for any empty price**, so "contact seller" and time-credit-only
  listings were labelled Free on every list.
- **"Confirm delivery" released the seller's money on one untitled tap**, beside "Dispute".

Plus: offer actions had no busy state and no confirmation; orders and offers had no
pull-to-refresh although payment hand-offs return to them; coupon minimum-order and max-uses
were parsed with `Number()` on a decimal-pad field; the public coupon list showed "No
coupons" on a failed request; the pickup code was text the seller had to type while their own
tools tab offers a scanner; and the 3-D Secure return URL had no screen behind it.

## Findings fixed in this pass

Identifiers are `report/finding` as the auditors numbered them.

**Money and credits**
- B/F-02 — a time-credit transfer committed on one tap, and a deep link could name an
  unverified recipient. Now: confirmation dialog naming recipient and amount; the recipient
  id is resolved against the server and the URL's name is never shown.
- B/F-03 — donations had no idempotency key; a timed-out retry donated twice. Client sends
  one key per intended donation; `POST /v2/wallet/donate` replays a duplicate (content-bound
  key, 24 h). PHP tests: `WalletDonateIdempotencyTest`.
- B/F-04 — confirming exchange hours pre-filled the *proposed* figure even when the other
  member had confirmed a different one; a ≥0.25 h gap sends the exchange to dispute. Now
  pre-fills the counterparty's confirmed hours, formatted for the member's locale, with a
  warning sentence.
- B/F-17 — wallet action errors shown raw; now the server's reason via `describeApiError`.
- C/F-1 — completing a group exchange moved credits on one tap; confirming did too. Both ask.
- C/F-14 — unparseable per-participant hours/weights were sent as 0 / 1; validated first.

**Journeys that could not complete**
- B/F-01 — data export never reached the member (the app discarded the streamed file). New
  GET twin of the download route; the app downloads and hands the archive to the share
  sheet. PHP tests extended in `MemberDataExportTest`.
- A/F-01 — "Sign out" on the session-restore failure screen did nothing visible; the flag is
  now cleared on sign-out and on a token-less restore.
- A/F-02 — registration that signs the member straight in was challenged by the unsaved-
  changes guard.
- C/F-2 — requesting to join a private group showed "Joined", then silently reverted. The
  server's `pending` answer is now shown as "Requested", with the organisers' message.
- C/F-4 — a 403 during offline check-in sync purged the encrypted queue including never-
  synced check-ins. The queue is kept read-only; only the confirmed purge removes it.
- B/F-08 — the exchanges list was capped at 50 with no way to see the rest; paged now.
- B/F-09 — "Exchange active" on a listing was a dead-end toast and never refreshed; it now
  opens the exchange, and the checks re-run on focus.

**Refusals reported as failures**
- B/F-05 — a blocked / vetting / safeguarding refusal on sending a message read as "try
  again"; the server's sentence is shown and the composer closes.
- B/F-06 — a hidden or blocked profile read as "Could not load — Retry"; now "not
  available" / "you cannot view" with a way back. `useApi` exposes `errorStatus`/`errorCode`.
- B/F-07 — a refused exchange transition left stale buttons; the screen re-reads on
  404/409/422.
- B/F-14 — a deleted conversation read as a load error with Retry; now "no longer available".
- C/F-11 — an ordinary member deep-linking into the event editor got "Could not load event"
  with a Retry that failed for ever; now a refusal with the way back.
- C/F-13 — sixteen bare `catch {}` blocks across the event registration, safety, credential
  and offline check-in components discarded the server's reason.
- C/F-15 — any group-exchange load error read as "not found".

**Wrong or missing information**
- C/F-6 — event detail never showed the end time or end date of a timed event.
- C/F-7 — a cancelled/postponed event never showed the organiser's reason.
- C/F-9 — two raw translation keys on the group invite screen.
- C/F-10 — editing a `joinable` federated group silently downgraded it to `listed`.
- C/F-12 — six distinct offline-scan refusals read as one generic "invalid" sentence.
- C/F-16 — the group Events tab formatted in the device zone and showed 00:00 for all-day.
- A/F-06 — the inbox "N unread" chip summed the loaded page while the badge showed the
  server total; the inbox also ignored live messages. Now the server total, and the list
  re-reads when the badge changes.
- A/F-21, C/F-5 — the events tab discarded the server's reason, and a failed *later* page
  replaced the whole list; the list is kept with an inline retry for that page.
- C/F-18 — switching a tab or filter kept the previous rows on screen until the response.
- B/F-16 — choosing an auto-translate target changed the interface language, and there was
  no language picker at all. Side effect removed; Settings → App language added (7 locales).
- B/F-10 — marking notifications read did not update the tab badge.
- B/F-11 — tapping a notification for a switched-off module pushed a second Notifications
  screen on top of the first; now a notice, no navigation.
- B/F-12 — the thread jumped to the bottom on every incoming message and lost its place when
  older messages loaded.
- B/F-15 — "Delete for everyone" was offered on other people's messages.
- B/F-20, B/F-28, B/F-29, B/F-30 — thread avatar/title from the server, message-the-other-
  party carries the name, comma-locale prefill, dead typing indicator removed.
- A/F-05 — the keyboard's Return could fire a second submit on all four auth forms.
- A/F-10 — Home fetched and rendered the feed when the feed module was off.
- A/F-12 — a reset/verify link opened while signed out was replayed after sign-in.
- A/F-14, A/F-16, A/F-18, A/F-19, A/F-20, A/F-22, A/F-15 — 40 dp icon-only buttons, password
  toggle label, offline banner on four tabs, listing save race, swipe-archive double prompt,
  auth card centring, Explore stat number formatting.
- Mechanical: Explore sections now respect the listings and feed modules; the reaction bar
  honours reduced motion; a saved resource now opens on that resource.

## Findings NOT fixed in this pass

Open, and recorded in the status document's backlog:

- **C/F-3** group discussions cannot be opened or replied to (API exists; screen and client
  functions do not).
- **C/F-17** group admins cannot approve join requests or manage members from the app
  (needs confirmation against `parity-map.json`).
- **C/F-8** every group-detail tab shows the first page only (6 lists).
- **B/F-13** attachment sending shows no progress and cannot be cancelled
  (`uploadWithProgress` exists).
- **A/F-03** no way to resend the verification e-mail from the app (public endpoint exists).
- **A/F-04** registration 422s arrive as one sentence, never mapped to a field (server sends
  only the first error).
- **A/F-07** a closed-registration community still advertises "Create account" (needs
  `registration_mode` in the public bootstrap).
- **A/F-09** a failed signed-in community switch signs the member out first.
- **A/F-13** broken images render as blank blocks (no `onError`/placeholder anywhere).
- **C/F-11** (remainder) attendance, tickets, communications, history and blueprints still
  show a load error with Retry on a 403.
- **D/F-13** pickup-slot and coupon dates are typed by hand as `2026-06-01 10:00`, with the
  raw ISO string put back into the field when editing.
- **D/F-12** collections, saved searches and pickup slots delete with no confirmation, while
  coupons in the same screen do confirm.
- **D/F-11** the Stripe payments screen shows "not started" and 0.00 balances when its
  request failed.
- **D/F-9** an accepted offer shows only "View", never "pay now" or a deadline.
- **D/F-19 – D/F-23** currency mismatch between a shipping option and its listing, paid
  promotions that cannot be bought, a heart tap refetching three lists, merchant onboarding
  always restarting at step 1, and hardcoded "0.00" placeholders on comma locales.
- **C/F-19 – C/F-31**, **B/F-18 – B/F-27**, **A/F-11**, **A/F-17** — polish items; see the
  three reports' P3 sections (saved in the session scratchpad only; summarised in the status
  document).
- Whole modules not yet read: marketplace, volunteering/jobs/organisations, the gamification
  cluster, courses/podcasts/federation (last read 2026-09-06).

## Checks run

From `mobile/`: `type-check`, `lint` (0 errors, 0 warnings), `test --runInBand` (416 suites),
`check:untranslated` (0 against a ceiling of 0), `drift:check`, `verify:release`,
`verify:network-security`, `audit:production` (two reviewed exceptions unchanged),
`store:assets:check`. From the repository root, in the app container: the two changed PHP
test files (16 tests, 57 assertions) and PHPStan on the two changed controllers. Not run:
`budget:check` against a fresh bundle, the device workflows, the launch smoke.

---

## Second pass, same day — volunteering, jobs, organisations and the gamification cluster

The first pass left four modules unread and a list of open findings. This pass read the
two largest of those modules and worked the open list. Same rules: one auditor at a time,
every fix carries a test that was watched failing first, nothing walked on a device.

### What was read

- **Volunteering, jobs and organisations** (auditor E): sixteen screens including
  `volunteering.tsx` (2,202 lines), `jobs.tsx` (1,407) and `volunteering-org-dashboard.tsx`,
  plus their four API clients. 18 findings — 1 P0, 4 P1, 10 P2, 3 P3.
- **The gamification cluster** (auditor F): twenty screens including `gamification.tsx`
  (1,552 lines), goals, polls, reviews, endorsements, blog, resources, ideation, clubs and
  venues, plus eight API clients, cross-checked against seven Laravel controllers, two
  services and the schema dump. 19 findings — 5 P1, 14 P2.

### Fixed

**Group discussions are reachable at last.** The Discussion tab listed titles and reply
counts and went nowhere: the cards were not pressable, `lib/api/groups.ts` had no function
for either endpoint that serves a thread (`routes/api.php:665-666`), and no screen consumed
them. A member could start a discussion and never read a single answer to it, including
their own. New screen `group-discussion`, two new client functions, the cards now open the
thread. The strings were already translated into all seven languages, which suggests the
screen was planned once and lost.

**Money.**

- E/F-1 (P0) — an organisation wallet deposit sent no idempotency key, although
  `VolunteerController::walletDeposit` has read one since it was written. A deposit that
  timed out and was tapped again took the credits a SECOND time out of the member’s own
  personal wallet. One key per intended deposit now, reused across a retry; the generator
  moved to `lib/utils/idempotencyKey.ts`.
- E/F-2 (P1) — that same deposit moved credits on one tap and said nothing afterwards.
- F/F-1 (P1) — buying from the XP shop spent the XP on one tap with nothing in the API to
  reverse it. Deliberately no idempotency key: `GamificationV2Controller::purchase()` reads
  only `item_id`, so sending one would be theatre.
- E/F-3 (P1) — `new-job.tsx` STRIPPED the comma before parsing, so an employer offering
  "1,5" time credits published a vacancy offering 15 and a salary of "50,5" was posted as
  505. E/F-7 (P2) — four more fields rejected a comma decimal outright.

**Journeys that could not complete.**

- E/F-4 (P1) — the hiring pipeline could not make an offer, accept or reject. A
  `slice(0, 4)` always took the four lowest-indexed remaining stages, so everything past
  Shortlisted was unreachable and the fixed "Interview" button silently did nothing once
  they were already there.
- F/F-2 (P1) — the "Given" reviews tab was permanently empty. Both tabs were filtered out
  of one list and `ReviewService::getForUser()` scopes that query to `receiver_id`, so no
  row could ever match; `/v2/reviews/given` exists and is registered ahead of
  `/v2/reviews/{id}` for exactly this purpose.
- E/F-6 (P2) — logging volunteer hours hard-coded today with no field to change it, so a
  volunteer who worked yesterday could not record it at all, and a successful log said
  nothing — not that it worked, and not that the organisation must confirm the hours
  before they become credits.

**Members told things that were not true.**

- F/F-5 (P1) — every appreciation card printed raw translation keys. The card asked for
  the `profile` namespace, which has no `appreciations` block; the keys live in `members`,
  and there is no `fallbackNS`. The test file’s own i18n stand-in was namespace-blind,
  which is why nothing caught it; it is namespace-aware now.
- E/F-5 (P1) — a declined organisation registration looked approved: it slid out of the
  amber "awaiting approval" block into the ordinary managed list with a Manage button and
  no sign of the refusal, while "Create opportunity" stayed hidden with no explanation.
- F/F-9 (P2) — seven of the eight gamification loads threw their error away, so a failed
  badges call rendered "No badges yet" to a member who had earned ten.
- F/F-10 (P2) — no badge ever showed the date it was earned (the card read `earned_at`,
  the one column the server leaves null), and "Locked badges" read 0 for every member for
  ever (`/gamification/badges` returns only rows that exist, i.e. only earned ones).
- E/F-8, E/F-9 (P2) — three owner-only screens answered a 403 with "could not load" and a
  Retry that could never succeed. The organisation dashboard was worse: under that card
  every tab claimed "No pending applications", "No hours to review", "No transactions".
- F/F-13 (P2) — real admins never saw "Edit challenge", because the check matched
  `user.role` against names that are never written to that column.

**Irreversible taps and invisible failures.**

- E/F-11, E/F-12 (P2) — withdrawing a job application, cancelling a shift and withdrawing
  a volunteering application all happened on one tap. The volunteering DETAIL screen has
  confirmed the identical shift call since S4-16; the hub was never brought along.
- F/F-12 (P2) — a failed vote on an idea wrote into a status line inside a card further up
  the page, usually off screen, and the Vote button stayed live during the round trip while
  the server toggles the vote.
- F/F-11 (P2) — the shop balance froze after one purchase because the optimistic guesses
  were never cleared and were preferred over fresh server data.
- E/F-14 (P2) — a new vacancy did not appear in My Postings until a pull to refresh.
- E/F-16 (P2) — four failure paths showed the raw error instead of `describeApiError`.
- The job card’s salary period was hardcoded English: "yr", "mo", "hr".

### Still open after this pass

From the volunteering report: F-10 (the apply sheet never mentions the saved CV), F-13
(six volunteering lists stop at twenty rows), F-15 (registering an organisation lands on a
hub that may not show it), F-17 (an organisation website link can reject unhandled), F-18
(a failed load in edit mode leaves a permanently dead form), and the P3 polish list.

From the gamification report: F-3 (a member can never endorse anyone — `endorseSkill` has
zero callers), F-4 (a member can never send an appreciation — the wall is read-only), F-6
(six more lists stop at their first page), F-7 (the leaderboard only ever shows the top 20),
F-8 (seven content screens still show a 4xx refusal as a failure with a Retry), F-14 (two
live searches fire a request per keystroke), F-15 (knowledge-base search only filters what
is already on screen), F-16 (logged goal progress can only go up), F-17 (voting on a poll
discards every page loaded), F-18 (four screens have no pull-to-refresh), F-19 (a member
cannot withdraw an idea or delete their own comment), F-20 (a failed refresh is silent
whenever the list already has rows), and the polish list.

Everything carried forward from the first pass that was not worked here stays open, listed
in the status document.

### Modules still never read

Courses, podcasts and federation were last read on 2026-09-06 and were not revisited.

### Checks run in this pass

From `mobile/`: `type-check`, `lint` (0 errors, 0 warnings), `test --runInBand` — **419
suites, 3,423 tests, all passing** — `check:untranslated` (0 against a ceiling of 0),
`parity:check`, `themes:check`, `budget:check` (15.41 MB against a 16.35 MB ceiling),
`verify:release`, `verify:network-security`, `audit:production` (the two reviewed
exceptions unchanged), `store:assets:check`, `check:store-audience`, `check:cert-pins`,
`doctor` (18/18). From the repository root: `check:docs` and `check-doc-scores.mjs`.

🔴 `api:check` (the third part of `drift:check`) reported **UNVERIFIED, not a pass**: its
route snapshot is stale because `routes/api.php` has uncommitted changes belonging to
another session. Nothing in this pass touched that file. Refreshing the snapshot needs the
local app container and would have swept up work that is not ours.

Nothing was walked on a phone or an emulator, so the readiness score in the status
document does not move.
